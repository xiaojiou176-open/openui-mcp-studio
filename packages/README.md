# Packages

Shared contracts, runtime substrate utilities, and reusable repository
capabilities live here.

- `packages/contracts`: reusable code-level contracts
- `packages/shared-runtime`: pure runtime substrate utilities
- `packages/runtime-observability`: shared redaction, logging, and evidence
  helpers
