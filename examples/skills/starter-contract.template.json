{
  "status": "starter-draft",
  "id": "openui.example.skill-id",
  "label": "Starter Skill Label",
  "summary": "Describe one builder-facing slice without claiming a shipped Skills product.",
  "starterPack": {
    "manifest": "examples/skills/public-starter.manifest.json",
    "installNote": "examples/skills/install-use-note.md",
    "audience": "Replace with the intended starter-pack audience.",
    "role": "Replace with the bounded starter-pack role.",
    "packageShape": "Describe the starter-pack shape.",
    "notFor": "Describe what this draft must not be mistaken for."
  },
  "invocation": {
    "primarySurface": "local-stdio-mcp",
    "mcpTool": "replace_with_current_tool_name",
    "openApiBridge": {
      "enabled": true,
      "note": "Compatibility bridge only. Do not treat this as a hosted API claim."
    },
    "repoLocalPacket": {
      "enabled": false,
      "command": "replace_if_needed",
      "note": "Maintainer-facing only."
    }
  },
  "inputs": [
    {
      "name": "workspaceRoot",
      "required": true,
      "description": "The repo-local workspace or target path the current tool reads from."
    }
  ],
  "outputs": [
    {
      "name": "result",
      "description": "Describe the bounded repo-side output."
    }
  ],
  "limitations": [
    "Replace with current-scope limitations.",
    "Do not claim plugin, marketplace, SDK, or hosted API support unless those lanes become real and verified."
  ],
  "proofAnchors": [
    {
      "path": "README.md",
      "why": "States the current builder-surface order."
    }
  ],
  "nextActivationBoundary": [
    "Define what shared docs, contracts, or verification must be updated before this draft can move beyond starter status."
  ]
}
